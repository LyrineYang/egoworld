[pytest]
addopts = -ra
markers =
    env_smoke: environment smoke checks (GPU/driver/ffmpeg/path)
    pipeline_smoke: minimal end-to-end pipeline smoke tests
    sam2_smoke: SAM2 integration smoke test
